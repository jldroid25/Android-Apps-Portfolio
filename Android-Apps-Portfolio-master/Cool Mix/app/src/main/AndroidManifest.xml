<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nevictus.android.coolmix">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/electropic6"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/electropic6"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Electronica"
            android:label="@string/electronica" />
        <activity
            android:name=".Cinematic"
            android:label="@string/cinematic" />
        <activity
            android:name=".PopSongs"
            android:label="@string/popsong" />
        <activity
            android:name=".JazzMusic"
            android:label="@string/jazz" />
        <activity
            android:name=".RocknRoll"
            android:label="@string/rock" />
        <activity android:name=".PotPourit"
            android:label="@string/potPourit"
            />
    </application>

</manifest>